apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt
  name: irazu
  labels:
    app: irazu
spec:
  rules:
    - host: andrewgingrich.us
      http:
        paths:
          - backend:
              serviceName: frontend
              servicePort: 4040
            path: /
    - host: www.andrewgingrich.us
      http:
        paths:
          - backend:
              serviceName: frontend
              servicePort: 4040
            path: /
  tls:
    - hosts:
      - andrewgingrich.us
      - www.andrewgingrich.us
      secretName: irazu-tls
